{% extends 'base.html.twig' %}

{% block body %}
    <h1>Product edit</h1>

   {% include 'ZiiwebEcommerceBundle:Pruduct:form.html.twig' %}

    <ul>
        <li>
            <a href="{{ path('product_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
